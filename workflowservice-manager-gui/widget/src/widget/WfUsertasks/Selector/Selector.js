define([
    "jscore/core",
    "./SelectorView"
    ], function(core, View) {

       return core.Widget.extend({

            View: View,

            onViewReady: function() {
            }
        });
});