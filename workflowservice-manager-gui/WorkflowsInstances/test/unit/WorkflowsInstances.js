/*global define, describe, it, expect */
define([
    'workflowsinstances/WorkflowsInstances'
], function (WorkflowsInstances) {
    'use strict';

    describe('WorkflowsInstances', function () {

        it('WorkflowsInstances should be defined', function () {
            expect(WorkflowsInstances).not.to.be.undefined;
        });

    });

});
