/*global define, describe, it, expect */
define([
    'allworkflowinstances/AllWorkflowInstances'
], function (AllWorkflowInstances) {
    'use strict';

    describe('AllWorkflowInstances', function () {

        it('AllWorkflowInstances should be defined', function () {
            expect(AllWorkflowInstances).not.to.be.undefined;
        });

    });

});
